import { ValidationArguments, ValidationOptions } from 'class-validator';
export declare type CustomValidateOptions<T = unknown> = {
    validator: (value: T, args: ValidationArguments) => boolean;
    message: string | ((validationArguments: ValidationArguments) => string);
};
export declare function CustomValidate<T = unknown>(options: CustomValidateOptions<T>, validationOptions?: ValidationOptions): PropertyDecorator;
